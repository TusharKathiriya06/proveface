## 1.0.0
* Initial release.
* Blink, smile, turn-left, turn-right, turn-up, turn-down challenges.
* Camera permission handled internally — no setup needed in consuming app.
* Passive anti-spoofing + screen/video replay detection.
* Typed `ProveFaceResult` with base64 captured image on success.
* `appBarColor` and `title` customisable from the call site.
